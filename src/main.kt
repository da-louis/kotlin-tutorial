import kotlin.math.*

val users = mapOf(
        1 to "Jane",
        2 to "John",
        3 to "Mike"

)

fun main (args:Array<String>) {
    println(PI)

    for((id,name) in users){
       println("ID:$id, Name:$name")
    }
}
